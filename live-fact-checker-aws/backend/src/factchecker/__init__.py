"""factchecker — stateless claim-verification backend.

Public seam: given a claim as text, return a verdict with evidence.
See ``verify.verify_claim`` and ``verify.extract_claims``.
"""

__version__ = "0.1.0"
